function [ acc ] = calcAccuracy( cM )
%CALCACCURACY Takes a confusion matrix amd calculates the accuracy

acc
for index = 1:size(cM,1)
   acc = acc + sum(cM(index,index)); 
end

acc = acc / sum(cM);
end

